# Backend Environment Variables Example
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env for your current environment
# 2. Update the values based on whether you're in development or production
# 3. Restart the server after making changes

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Use these values when running locally (npm run dev)

NODE_ENV=development
PORT=5000

# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/ase-tag-software

# Frontend URL (optional in development - localhost is auto-allowed)
CLIENT_URL=http://localhost:5173

# =============================================================================
# PRODUCTION CONFIGURATION  
# =============================================================================
# Use these values when deploying to production
# Uncomment and update the values below:

# NODE_ENV=production
# PORT=5000

# MongoDB Atlas (Cloud Database)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name?retryWrites=true&w=majority

# Deployed Frontend URL (REQUIRED in production for CORS)
# CLIENT_URL=https://your-frontend-domain.com

# =============================================================================
# IMPORTANT NOTES:
# =============================================================================
# - NODE_ENV determines which CORS origins are allowed
# - In development: localhost ports are automatically allowed
# - In production: Only CLIENT_URL origin is allowed (REQUIRED!)
# - CLIENT_URL should NOT have a trailing slash
# - Restart server after changing environment variables
# - Never commit .env files to version control
